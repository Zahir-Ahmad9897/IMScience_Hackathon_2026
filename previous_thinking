Now let me continue building the agents, orchestrator, and the main app.py. I'll build them in parallel:


agents/farmer_agent.py
agents/customer_agent.py
agents/doctor_agent.py
orchestrator.py
app.py (premium UI)
These are the core remaining files. Let me create them all simultaneously.


For the agents, each will be a node function that takes the KisanState and returns updated messages. They'll use the LLM with appropriate system prompts and tools.


For the orchestrator, I'll use LangGraph's StateGraph with:


A supervisor/router node that routes to the correct agent based on user_role


Each agent node handling its specific domain


A shared tools node


Conditional edges routing back from tools to the respective agent


I'm visualizing the flow: the supervisor routes based on user_role to the appropriate agent, which processes the request with its own LLM and tools, then conditionally edges back through the tools node before returning to the supervisor. For simplicity and correctness with LangGraph, I'll bind the LLM with different system prompts and tools to each agent node, letting the supervisor route to the right one based on state.user_role.


Now I'm working through the routing logic—the supervisor returns the user_role to determine which agent handles the request, and each agent can call tools before either returning to the supervisor or looping back through the tools node. I'm considering whether to use separate agent nodes for each role or consolidate into a single agent node that switches behavior based on user_role, and whether to handle the loop-back to the same agent cleanly in LangGraph. For routing back to the correct agent after tool execution, I'll use a conditional edge that checks the current_agent state. My final architecture has the supervisor setting the agent type, then dispatching to the appropriate node, which either ends if there's no tool call or loops back through tools and returns to the dispatcher. Now I'm writing out the implementation starting with the main orchestrator graph and individual agent modules.


For the Streamlit app, I'm designing a premium interface with a dark theme and green agricultural accents, role selector cards with icons, bilingual support for English and Urdu, voice recording and image upload capabilities, and animated gradients throughout. Each agent will have access to specific tools—the farmer gets weather and crop analysis, the customer gets product search, and the doctor gets health diagnostics.


Writing the agent modules and UI... Setting up the import structure across the project files, where each agent module exports a factory function that takes the LLM and returns a configured node, and the orchestrator will wire everything together with the graph and state management. Building the agent node factories...


Now I'm designing the Streamlit UI with a premium look—header with the KisanAI branding and gradient styling, a sidebar for role selection, language toggling, voice options, and thread management, plus the main conversation area.


For the interface itself, I'm planning dark theme colors with green and teal accents, glassmorphism cards, gradient text effects, and smooth animations. The main chat area will display role-specific avatars, support image uploads with drag-and-drop, include a voice recorder button, and handle Urdu text input. I need to handle RTL text direction for Urdu messages and leverage Streamlit's audio input feature for voice recording.


Now I'm organizing the file structure into batches. First, I'll set up the package initialization files and create the agent modules for the farmer, customer, and doctor roles. I also need to update requirements.txt to include OpenAI and LangChain dependencies. Then I'll build the orchestrator that ties everything together, followed by the main app file that imports from all these modules.


The app itself will be substantial—around 500 lines—since it needs to handle role selection, language toggling, image uploads with previews, voice input, proper RTL support for Urdu text, text-to-speech playback, thread management in the sidebar, and streaming responses with custom styling. The orchestrator's main challenge is managing the routing logic after tool execution in LangGraph.


I'm thinking through the multi-agent architecture: I could either have one agent node that dispatches internally based on user role, or use separate agent nodes with a routing function that checks the current agent state. The cleaner approach is having an orchestrator node that sets the current agent, then three specialized agent nodes, with routing back through the current agent after tools execute. I'm sketching out the graph structure now. I see a cleaner approach: instead of routing back to specific agent nodes after tool execution, I can use a single dispatch node that checks the current agent and routes accordingly. The key is checking whether there are pending tool calls—if so, loop back to the same agent; otherwise, end the flow.


Setting up the conditional edges... The user_role gets passed into the initial state when invoking the graph, so the START → agent_router routing works as expected. I'm second-guessing whether add_conditional_edges(START, ...) is fully supported in LangGraph, so I'm considering adding a supervisor node as the first step to be safe—it would sit between START and the conditional routing to the different agent nodes.


From there, each agent node routes through a tools condition that either executes tools or ends, and tools feed back to the agent router to continue the conversation. Now I'm ready to write out the full implementation. For the Streamlit app, I'll use native components like chat input and audio input, add a dark theme with glassmorphism styling, put the role selector in the sidebar, handle language auto-detection with a manual override option, and support image uploads and TTS output.


I need to set up the package structure with empty init files, then write the agent-specific system prompts that are tailored to each domain—the farmer agent especially needs detailed knowledge of Pakistani crops and should respond in whatever language the user chooses.


Writing the agent system prompts... the user writes in English, respond in English. I need to provide specific diagnoses with treatment protocols, mention Pakistani pesticide brands and dosages, ask about location for region-specific pest pressure, and always recommend safety intervals before harvest.


Now I'm setting up the state management for the multi-agent system, defining a TypedDict that tracks the conversation history, which agent is currently active, the user's role and language preference, and any image data they've uploaded for analysis.


I'm creating a supervisor node that simply routes to the appropriate agent based on the user's role without needing an LLM call, then setting up routing functions that direct the flow to the correct agent and back after tool execution. The supervisor acts as a lightweight dispatcher while each specialized agent handles its own LLM reasoning with role-specific prompts and tools. I can skip the supervisor node entirely and route directly from the start based on the user_role, then have the agent nodes handle tool calls and loop back as needed. For the image handling in the app, I need to base64 encode the uploaded file and include it in the user message so the agent knows to call the analyze_crop_image tool with that data. tool needs access to the base64 data, so I'll store it in the agent state and reference it when constructing the tool call message.


Actually, a cleaner approach would be to run the image analysis upfront using GPT-4o vision directly before the agent even starts, then inject that analysis as context. This way any agent can reference the pre-computed analysis without needing to call the vision tool themselves. I'll implement it by analyzing the uploaded image immediately, storing the result in session state, and prepending it as a system message when the user sends their question.


Now I'm ready to write out all the code files. I'll use Streamlit's built-in audio input to avoid external dependencies. Now I'm laying out the full app structure with the UI components—custom dark theme styling, the header and branding, sidebar controls for role and language selection, the main chat interface, image upload capability, and voice input handling. For the orchestrator, I'm defining how each agent node returns its state updates, starting with the farmer agent that pulls in the system prompt and any image context.


Writing the complete application files... I'm mapping out the voice interaction flow—recording audio, transcribing it with Whisper, displaying the text, sending it to the agent, and playing back the response with TTS if enabled. For the orchestrator, I need to track which agent is active in the state so the system knows where to route back after tool calls, using a conditional edge that checks the user's role to determine the starting agent.


Setting up the graph routing... I'm deciding between having each agent create its own LLM instance versus having the orchestrator create one shared instance and pass it to the agents. The shared approach is cleaner and more memory-efficient, so I'll go with that—orchestrator.py handles the LLM and tool binding, while each agent module just defines its tools and system prompt, then receives the configured LLM through a node factory function.


Writing the agent module structure... Assigning tool subsets to each agent based on their role, then binding them to the language model. I'm noticing a potential issue though—if the farmer agent calls a tool that wasn't included in its subset, the tool node won't have access to it since it only knows about the tools that were bound to that specific LLM instance